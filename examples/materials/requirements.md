# Northstar · 任务与商品服务

## 目标
为小团队提供任务列表，同时对商品目录和购物车提供可测试 API。

## 验收标准
- 输入非空标题后创建任务，任务立即显示在列表。
- 空标题必须返回 422，页面展示标题必填提示。
- GET /api/products 返回商品数组和总数。
- POST /api/cart 的 total 等于单价乘数量；数量必须为正整数。
- 未知资源返回 404。

## 自动化范围
Web 覆盖创建、空值、列表累加和目录展示；API 覆盖状态码、响应结构、边界参数与金额计算。
